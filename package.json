{
  "name": "kanban-board",
  "version": "1.0.0",
  "description": "Kanban board with JWT authentication",
  "scripts": {
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd server && npm run build && cd ../client && npm run build",
    "start": "cd server && npm start",
    "seed": "cd server && npm run seed"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
